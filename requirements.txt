# ── MV AutoML Studio — dependencias ────────────────────────────────────────────
# Núcleo web
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6
orjson>=3.9

# Datos (out-of-core / sin límite de tamaño)
pandas>=2.1
numpy>=1.26
pyarrow>=15.0
duckdb>=0.10
chardet>=5.2

# Excel
openpyxl>=3.1
xlsxwriter>=3.1

# Machine Learning
scikit-learn>=1.4
optuna>=3.5
shap>=0.44
scipy>=1.11
lightgbm>=4.1
xgboost>=2.0
catboost>=1.2
statsmodels>=0.14
joblib>=1.3

# matplotlib no se pide directo: la trae catboost, sin techo. shap arma su
# paleta mutando un colormap (set_bad/set_over/set_under) con métodos que
# matplotlib ya viene avisando como deprecados desde 3.9; el día que ese aviso
# pase de PendingDeprecationWarning a error, `import shap` se cae entero y el
# informe se queda sin explicación de variables. El código roto está adentro
# de shap, no acá, así que no hay parche de este lado — sólo contener el daño:
# techo en la versión más nueva ya probada, para no subir a una que rompa sola
# en un `pip install` de rutina. `backend/tests/test_dependencias.py` vigila
# el día que shap salga una versión que ya no mute el colormap, para sacar
# este techo.
matplotlib<3.12

# Conectores SQL
sqlalchemy>=2.0
pymysql>=1.1
psycopg2-binary>=2.9
pymssql>=2.2
# Fabric y Azure SQL se autentican con Entra ID, y eso pymssql no lo habla: ese
# camino va por ODBC. El driver del sistema («ODBC Driver 18 for SQL Server») lo
# instala Microsoft aparte; esto es sólo el puente de Python.
pyodbc>=5.0

# Proveedores de IA (HTTP genérico, sin SDKs propietarios)
httpx>=0.26

# Licencias (firmas Ed25519)
cryptography>=41.0
